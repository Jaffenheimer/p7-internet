FROM node:18-alpine as node
WORKDIR /src

#Copy Files
COPY src/P7Internet.Frontend/react-app/package*.json .
RUN npm install
COPY P7Internet.Frontend .

# RUN npm run-script build


#Copy files from GitHub to nginx container
FROM nginx:alpine

COPY --from=node /P7Internet.Frontend/ /usr/share/nginx/html

# RUN rm /etc/nginx/conf.d/default.conf
# COPY nginx/nginx.conf /etc/nginx/conf.d

# EXPOSE 80

# CMD ["npm", "start"]

CMD ["nginx", "-g", "daemon off;"]
